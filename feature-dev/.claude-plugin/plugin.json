{
  "name": "feature-dev",
  "version": "2.11.7",
  "description": "コードベース理解・アーキテクチャ設計・runtime smoke test・品質レビューを 8 phase で進める機能開発ワークフロー。code-explorer / code-architect の 2 agent を内包し、型・テスト・lint の決定的オラクルゲートを高コストレビューの前に置く。Phase 6 は code-review:self-review へ --embed 委譲する。",
  "_requirements": [
    {
      "name": "code-review",
      "type": "plugin",
      "required": false,
      "description": "Phase 6 品質レビュー委譲先。未インストール時は Phase 6 が動作しない（fail-fast）"
    },
    {
      "name": "bdd-spec",
      "type": "plugin",
      "required": false,
      "description": "Phase 1.3 BDD spec 入力契約。インストール時のみ create-spec を呼び spec.md を architect 入力に使用、未インストール時は既存 Issue 解釈フローに fallback"
    },
    {
      "name": "design-doc",
      "type": "plugin",
      "required": false,
      "description": "Phase 4.5 design doc export。インストール時のみ採用案 + 代替案比較を export 非対話 API で .claude/designs/ に永続化、未インストール時は skip"
    },
    {
      "name": "kvault",
      "type": "cli_tool",
      "required": false,
      "description": "knowledge vault CLI。Phase 1.6 Vault Recall で横断知見を recall し architect に advisory 注入、未設定/未導入時は skip（KNOWLEDGE_VAULT_ROOT 環境変数で vault 位置を指定）"
    }
  ],
  "author": {
    "name": "yuki"
  }
}
